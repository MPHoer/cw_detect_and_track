# Recovered detector versions + trackpy 0.7 tested during packaging.
# The combined original-script environment has not yet been tested.
# Python 3.11 recommended. Install a torch build appropriate for your hardware.
rfdetr==1.3.0
torch==2.9.1
torchvision==0.24.1
supervision==0.27.0
numpy==2.2.6
pandas==2.3.3
Pillow==11.3.0
opencv-python==4.12.0.88
trackpy==0.7
scipy>=1.11,<2
matplotlib>=3.8,<4
tqdm>=4.66
wandb==0.23.0
# ASM preprocessing additionally requires the original fringe.solvers.AngularSpectrum.
# Its exact distribution/version is not recovered; do not install an unrelated package by name.
